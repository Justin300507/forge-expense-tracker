databases:
  - name: forge-expense-tracker-db
    databaseName: forge_expense_tracker_db
    plan: free

services:
  - type: web
    name: forge-expense-tracker-backend
    runtime: python
    plan: free
    buildCommand: pip install -r app/requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: forge-expense-tracker-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true

  - type: web
    name: forge-expense-tracker-frontend
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false
